#ifndef IMPLEMENTATOR_H
#define IMPLEMENTATOR_H

bool isPrime(unsigned long n); // Checks if n is prime
unsigned long nextPrime(unsigned long N); // Finds the first prime number greater than or equal to N

#endif