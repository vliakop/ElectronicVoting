#ifndef VALIDATOR_H
#define VALIDATOR_H

bool keyValidator(char *); // Validates if the arguements if correct (not-NULL)
bool recordValidator(char *); // Validates if the fields of the record are correct (not-NULL)

#endif
