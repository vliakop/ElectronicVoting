* runelection.cpp : Main

Term project: An attempt to immitate an e-voting system.
Constraints: Implementation in C/C++, use of STL library forbidden

Data Structures implemented:
 1. A Bloomfilter
 2. A Reb-Black Tree
 3. Custom classes/structs to represented the entities of the project requirements.

Hash functions used:
 1. ascii hash
 2. djb2
 3. jenkins one at a time

The following pipeline for the commandline user interface has been implemented:
 1. Parser (user input parsing based on a set of acceptable commands)
 2. Validator (validates the syntax of the command parsed by Parser)
 3. Assigner (assigns the execution of the validated command to proper implementator)
 4. Implementator
